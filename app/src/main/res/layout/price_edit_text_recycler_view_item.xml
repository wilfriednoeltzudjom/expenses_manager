<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="false"
    android:gravity="center"
    android:orientation="vertical">

    <com.thetechshrine.expensemanager.components.views.ExtendedTextView
        android:id="@+id/price_edit_text_recycler_view_item_value"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_price_edit_text_keyboard_item"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:padding="16dp"
        android:textColor="@color/colorPrimary"
        android:textSize="18sp"
        app:font_weight="SemiBold" />

    <ImageButton
        android:id="@+id/price_edit_text_recycler_view_item_clear"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_price_edit_text_keyboard_item"
        android:contentDescription="@string/backspace"
        android:padding="16dp"
        android:src="@drawable/ic_backspace" />

</LinearLayout>
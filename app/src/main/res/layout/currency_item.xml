<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:id="@+id/currency_item_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:background="@drawable/background_selectable_category"
    android:padding="24dp"
    android:gravity="center">

    <com.thetechshrine.expensemanager.components.views.ExtendedTextView
        android:id="@+id/currency_item_currency_code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/colorBlack"
        android:textSize="20sp"
        app:font_weight="SemiBold" />

    <TextView
        android:id="@+id/currency_item_currency_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="4dp"
        android:textColor="@color/colorGray"
        android:textSize="13sp" />

</LinearLayout>
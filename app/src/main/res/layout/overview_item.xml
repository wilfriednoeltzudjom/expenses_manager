<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overview_item_container"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_marginEnd="8dp"
    android:gravity="center|bottom">

    <LinearLayout
        android:id="@+id/overview_item_amount_container"
        android:layout_width="32dp"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:weightSum="100"
        android:orientation="vertical"
        android:background="@android:color/transparent"
        android:gravity="bottom">

        <LinearLayout
            android:id="@+id/overview_item_amount"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:orientation="vertical"
            android:background="@drawable/background_overview_item_shadow_gray" />

    </LinearLayout>

    <TextView
        android:id="@+id/overview_item_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/colorDarkGray"
        android:textSize="12sp"
        android:text="01/10" />

</LinearLayout>